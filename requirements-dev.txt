# Development dependencies for KE-WP Mapping Application
# Install with: pip install -r requirements-dev.txt
# NOTE: Main requirements.txt already includes pytest, pytest-cov, pytest-flask

# Code Quality & Linting (compatible versions for our Flask setup)
flake8==6.0.0
black==26.3.1
isort==5.12.0
mypy==1.5.1
pylint==2.17.7
ruff==0.15.0

# Security Analysis (pinned to avoid conflicts)
bandit[toml]==1.7.5
safety==3.0.1
pip-audit==2.10.0

# Additional Testing Tools (avoiding conflicts with main requirements)
pytest-mock==3.12.0
pytest-xdist==3.5.0
pytest-benchmark==4.0.0

# Code Complexity Analysis (lightweight tools only)
radon==6.0.1
xenon==0.9.1

# Documentation (optional tools)
pydocstyle==6.3.0

# Development Tools (essential only)
pip-licenses==4.3.2
pre-commit==3.4.0

# Database Tools (compatible with our SQLite setup)
sqlite-utils==3.39

# API Testing (compatible with our requests version)
requests-mock==1.11.0
responses==0.24.1