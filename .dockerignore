# Git
.git
.gitignore

# Python
__pycache__
*.pyc
*.pyo
*.pyd
.Python
env
pip-log.txt
pip-delete-this-directory.txt
.tox
.coverage
.coverage.*
.pytest_cache
nosetests.xml
.cache
coverage.xml
*.cover
*.log
.venv

# Node
node_modules

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode
.idea
*.swp
*.swo

# Project specific
# Use **/ — .dockerignore patterns are relative to the context root, so a bare
# `*.db` does NOT match data/ke_mappings.db and the database would be baked into
# the image. At runtime /app/data is bind-mounted over in production, but a
# stray DB still ships in the image and is visible to anything that reads it
# before the mount (and to anyone who pulls the image).
**/*.sqlite
**/*.db
dataset.csv.backup
proposals/*.txt
logs/
.env
flask_app.log

# Test files
pytest.ini
.pytest_cache/
tests/